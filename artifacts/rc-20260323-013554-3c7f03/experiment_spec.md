# Experiment Specification

I need write permission to `.gitignore`. Once granted, I'll add `artifacts/` to it, then stage `config.concordant.yaml` + `.gitignore`, commit, and push. Please approve the edit.