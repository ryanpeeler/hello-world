I need your approval to commit and push. The plan is:

1. Stage `config.carbon-nanotubes.yaml` (skipping `artifacts/` and `__pycache__/` dirs as they are generated outputs)
2. Commit all staged files (the `researchclaw/` package + `tests/` + config)
3. Push to remote branch `claude/run-carbon-nanotubes-pipeline-oIbBL`

Please approve the tool calls when prompted, or let me know if you'd like to adjust what gets committed.