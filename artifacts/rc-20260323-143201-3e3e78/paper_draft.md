I can see the staged files. Before committing, I want to confirm what you'd like included. The staged files are the `researchclaw/` package and `tests/`. There are also untracked files:

- `config.carbon-nanotubes.yaml` — project config
- `artifacts/` — run artifact directories
- `__pycache__/` directories — should **not** be committed

Should I commit just the staged files (researchclaw + tests), and also add `config.carbon-nanotubes.yaml`? I'll skip `artifacts/` and `__pycache__/` unless you want them included.