I need your approval to stage and commit. To proceed, I'll:

1. `git add config.carbon-nanotubes.yaml`
2. Commit the staged files (`researchclaw/`, `tests/`, and the config) — skipping `artifacts/` and `__pycache__/`
3. `git push`

Please approve the git commands to continue.